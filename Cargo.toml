[package]
name = "insn_tree"
version = "0.1.0"
edition = "2021"

[dependencies]
bitflags = { version = "2.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

